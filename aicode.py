# Create an app to detect faces in realtime.# In the next exercise, you will use the opencv detect_faces function, which# detects faces in an image and tracks them. import cv2import numpy as np# Create an instance of the face_recognizer classface_recognizer = cv2.face.LBPHFaceRecognizer_create()# Track the face in realtimewhile True:    # Check if a face is recognized    # Get the frame from the video stream    frame = cv2.QueryFrame(stream)        # Resize the webcam capture to the greater of the width and height of the current frame    frame = cv2.resize(frame, (frame.shape[1], frame.shape[0]))        # Convert the frame from RGB to grayscale    gray = cv2.cvtColor(frame, cv2.COLOR_RGB2GRAY)        # Augment    gray = cv2.cvtColor(gray, cv2.COLOR_GRAY2BGR)        # Evaluate the gray level of each pixel    gray = cv2.cv.CV_32FC1    gray = cv2.cv.create_filter(cv2.CV_32FC1, cv2.cv.CV_32FC1)    gray = cv2.cv.cvtColor(gray, cv2.COLOR_BGR2GRAY)        # Find the face in the entire gray level image    is_face = face_recognizer.detect(gray, is_face)        # If the fac